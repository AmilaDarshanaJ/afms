<p style="color: green"><%= notice %></p>

<%= render @crop_type %>

<div>
  <%= link_to "Edit this crop type", edit_crop_type_path(@crop_type) %> |
  <%= link_to "Back to crop types", crop_types_path %>

  <%= button_to "Destroy this crop type", @crop_type, method: :delete %>
</div>
